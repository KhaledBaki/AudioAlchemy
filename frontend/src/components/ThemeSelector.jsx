// ThemeSelector is now embedded inside MainMenu — keeping this for compatibility
import React from "react";

function ThemeSelector({ selectedTheme, setSelectedTheme }) {
  return null;
}

export default ThemeSelector;
